Installation
============

Download the project and at the command line (in the project directory)::

    $ python setup.py install

OR

At the command line::

    $ easy_install nwispy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nwispy
    $ pip install nwispy
