============
Installation
============

At the command line::

    $ easy_install python-boilerplate

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-boilerplate
    $ pip install python-boilerplate
